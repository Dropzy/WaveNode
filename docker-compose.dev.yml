services:
  backend:
    build:
      context: ./Backend
      args:
        VERSION: ${WAVENODE_VERSION:-0.1.8}

  frontend:
    build:
      context: ./Frontend
      args:
        VERSION: ${WAVENODE_VERSION:-0.1.8}

  updater:
    build:
      context: ./Updater
