services:
  backend:
    build:
      context: ./Backend
      args:
        VERSION: ${WAVENODE_VERSION:-0.1.2}

  frontend:
    build:
      context: ./Frontend
      args:
        VERSION: ${WAVENODE_VERSION:-0.1.2}

  updater:
    build:
      context: ./Updater
